# @package _global_
model:
  min_ring_length: 4
  constraints:
    - type: ring_length_at_least
      min_ring_length: ${model.min_ring_length}
  use_projection: true
general:
  sampling_output_dir: samples/${general.name}/count_disabled_length_4/seed_${train.seed}
